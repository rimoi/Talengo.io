{% if is_granted('microservice_edit', commande.microservice) %}
    {% if commande.deliver == true %}
        <div class="card my-4" id="rapport">
            <div class="card-header bg-dark">
                <h6 class="h6 text-light">
                    Rapport de fin de prestation de service
                </h6>
            </div>
            <div class="card-body">
                {% if rapport == false %}
                    <div class="pt-0">
                        {{ form_start(rapportForm) }}

                        {{ form_widget(rapportForm) }}

                        <button type="submit" class="btn btn-dark">
            <span id="btnFetch" class="w-100">
               <i class="mdi mdi-send"></i> Envoyer
            </span>
                        </button>

                        {{ form_end(rapportForm) }}
                    </div>
                {% else %}
                    <p class="font-italic p-3 bg-light pt-0 d-block rounded">
                       Nous accusons réception de votre rapport et vous remercions pour votre collaboration.
                    </p>
                {% endif %}
            </div>
        </div>
    {% endif %}
{% endif %}
{% extends 'vendeur.html.twig' %}

{% block title %}Mise à jour de #{{ realisation.name }}{% endblock %}

{% block body %}
    <div class="p-3 border-bottom text-left bg-dark rounded">
        <h4 class="mb-0 h6 text-light">
            {{ block('title') }}
        </h4>
    </div>

    {{ include('vendeur/realisations/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="text-right mt-2">
        {{ include('vendeur/realisations/_delete_form.html.twig') }}
    </div>

{% endblock %}